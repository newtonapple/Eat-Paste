.paste{:id => dom_id(paste)}
  %span.language= "#{paste.default_language}"
  = link_to 'more...', paste
  = timeago(paste.created_at, :class=>'created_at')
  .paste-body
    ~ paste.preview
  = render :partial => 'tags', :object => paste.tags